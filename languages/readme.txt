This directory contains language translation files for the website.
Each language has its own JSON file with translations for all text content.

The language switching is implemented in the language.js file in the root directory.